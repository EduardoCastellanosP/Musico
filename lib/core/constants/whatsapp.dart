/// Short label for WhatsApp contact buttons app-wide — keeps the pill/CTA
/// compact instead of the full "WhatsApp" wordmark. Single source so every
/// contact button stays in sync if the copy changes.
const String kWhatsAppShortLabel = 'WhatsApp';
